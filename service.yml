apiVersion: v1
kind: Service
metadata: 
  name: demo
  labels: 
    app: demo
spec: 
  ports: 
    - port: 8080
      targetPort: 80
      nodePort: 31200
  selector: 
    app: demo
  type: LoadBalancer
